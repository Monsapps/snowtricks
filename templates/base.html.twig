<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
		<link rel="stylesheet" href="{{ asset("css/style.css") }}">
		<script src="{{ asset("js/main.js") }}"></script>
		<script src="{{ asset("js/medias-providers.js") }}"></script>
		<title>{% block title %}SnowTricks - The snowboard freestyle community{% endblock %}</title>
		<meta name="description" content="The snowboard freestyle community, share and create tricks">
		<meta name="keywords" content="snowboard, trick, freestyle, snowtricks">
    </head>
    <body>
		<header class="container">
			<nav class="navbar navbar-light bg-light d-none d-md-block">
				<div class="container-fluid">
					<div class="col">
						<a class="navbar-brand" href="{{ path("homepage") }}">SnowTricks</a>
					</div>
					<div class="col d-flex justify-content-end">
						<a class="nav-link" href="{{ path("homepage") }}">Home</a>
						<a class="nav-link" href="{{ path("tricks") }}">Tricks</a>
                        {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                            <a class="nav-link" href="{{ path("signout") }}">Sign out</a>
                        {% else %}
                            <a class="nav-link" href="{{ path("signin") }}">Sign In</a>
                            <a class="nav-link" href="{{ path("signup") }}">Sign Up</a>
                        {% endif %}
					</div>
				</div>
			</nav>
			<!-- Navbar for mobile view -->
			<nav class="navbar fixed-bottom navbar-light bg-light d-block d-md-none">
				<div class="row">
					<div class="col text-center">
						<a class="nav-link" href="{{ path("homepage") }}"><img src="https://img.icons8.com/ios-glyphs/30/000000/home.png"/></a>
					</div>
					<div class="col text-center">
						<a class="nav-link" href="{{ path("tricks") }}"><img src="https://img.icons8.com/ios-glyphs/30/000000/snowboarding.png"/></a>
					</div>
                    {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                        <div class="col text-center">
						    <a class="nav-link" href="{{ path("signout") }}"><img src="https://img.icons8.com/ios-glyphs/30/000000/exit.png"/></a>
					    </div>
                    {% else %}
                        <div class="col text-center">
                            <a class="nav-link" href="{{ path("signin") }}"><img src="https://img.icons8.com/ios-glyphs/30/000000/enter-2.png"/></a>
                        </div>
                    {% endif %}
				</div>
	  		</nav>
		</header>

        {% for message in app.flashes("positive-response") %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes("negative-response") %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        {% block body %}{% endblock %}

        <div class="d-md-none d-lg-none d-xl-none">
			<br><br>
        </div>
		<footer class="container">
			<div class="navbar navbar-light bg-light">
				SnowTrick &copy; 2021
			</div>
		</footer>
	</body>
</html>
